#include <stdio.h>
#include <stdlib.h>

/**
 * main - prints the name of a file
 *
 * Return: Always 0 (Success)
 */

int main(void)
{
	printf("the name of the file is: %s\n", __FILE__);
	return (0);
}
